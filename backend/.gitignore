Node.js / Express Backend .gitignore
Dependencies
Should be installed via 'npm install' on the server/other machines
/node_modules

Environment Variables
NEVER commit your secrets file. Create a .env.example file with dummy values instead.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

Logs
Log files can contain sensitive information and grow very large.
logs
.log
npm-debug.log
yarn-debug.log*
yarn-error.log*

Production / Build output
If you ever use a transpiler like TypeScript, the output is generated.
/build
/dist

Testing
Test coverage reports are generated on each run.
/coverage
/lcov-report

Editor / IDE specific files
.vscode/
.idea/
*.suo
.ntvs
*.njsproj
*.sln
*.swp

OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db